{
  "name": "warpjs-starter",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "dev": "npx npm-run-all --parallel dev:*",
    "dev:server": "warp dev .",
    "dev:client": "npx live-server public/ --no-browser",
    "build": "warp build .",
    "deploy": "warp deploy ."
  },
  "dependencies": {
    "axios": "^0.21.1"
  },
  "devDependencies": {
    "warp": "^4.0.4"
  },
  "optionalDependencies": {
    "live-server": "^1.2.1",
    "npm-run-all": "^4.1.5"
  }
}
